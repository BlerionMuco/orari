import * as React from "react";
import { BookingNotFound } from "@/components/booking/states/booking-not-found";

export default function NotFound(): React.JSX.Element {
  return <BookingNotFound />;
}
